<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/tips_point"
        android:layout_width="8dp"
        android:layout_height="3dp"
        app:layout_constraintStart_toStartOf="@id/ll_container_item"
        android:src="@mipmap/icon_dialog_arrwo_up"
        app:layout_constraintEnd_toEndOf="@id/ll_container_item"
        app:layout_constraintTop_toTopOf="parent" />

    <LinearLayout
        android:id="@+id/ll_container_item"
        android:background="@drawable/bg_dialog_tips"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tips_point"
        android:layout_width="wrap_content"
        android:showDividers="middle"
        android:dividerPadding="@dimen/dp_10"
        android:divider="@drawable/shape_diviver_line_1px_line_color"
        android:layout_height="wrap_content"
        android:orientation="vertical" />

<!--    tools:listitem="@layout/dialog_value_tips_child"-->

</androidx.constraintlayout.widget.ConstraintLayout>